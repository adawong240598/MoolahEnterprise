import { PointOfContactEntity } from './point-of-contact-entity';

describe('PointOfContactEntity', () => {
  it('should create an instance', () => {
    expect(new PointOfContactEntity()).toBeTruthy();
  });
});
