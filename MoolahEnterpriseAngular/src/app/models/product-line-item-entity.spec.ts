import { ProductLineItemEntity } from './product-line-item-entity';

describe('ProductLineItemEntity', () => {
  it('should create an instance', () => {
    expect(new ProductLineItemEntity()).toBeTruthy();
  });
});
