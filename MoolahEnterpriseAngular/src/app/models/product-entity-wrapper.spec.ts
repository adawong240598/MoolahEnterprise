import { ProductEntityWrapper } from './product-entity-wrapper';

describe('ProductEntityWrapper', () => {
  it('should create an instance', () => {
    expect(new ProductEntityWrapper()).toBeTruthy();
  });
});
