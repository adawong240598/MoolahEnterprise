import { CategoryPricingEntity } from './category-pricing-entity';

describe('CategoryPricingEntity', () => {
  it('should create an instance', () => {
    expect(new CategoryPricingEntity()).toBeTruthy();
  });
});
